package main

import (
	"fmt"
	"log"
)

// C. Даты https://contest.yandex.ru/contest/28730/problems/C/

func main() {
	// * число --
	var N, i, j int
	_, err := fmt.Scan(&N, &i, &j)
	if err != nil {
		log.Fatal(err)
	}

}
